# FlexCharts API

## Running locally

- Install dependencies: `npm install`

- Install serverless globally: `npm install serverless -g`

- Create a local mysql database, note the db name and login

- Run each migration script from the `/migrations` folder, this will be automated soon!

- Copy the example .env and fill in with your local credentials

- Run serverless: `npm start`
